
確認事項

見ているものが古いものの可能性があるのでその場合はご容赦を

--------------------------------------------------------------------------------

□ KK01001Form
・バリデーションかけられないので記述は削除していいかも

対応：バリデーションを削除しました。

--------------------------------------------------------------------------------

□ KK05001Controller
・changePasswordForm
	パスワード変更画面に遷移するために必要で合っている？

対応：パスワード変更には使用しておらず、不要なメソッドだったので削除しました。

・ユーザ登録画面へ遷移させるクラスを作っていないのは何か理由がある？

対応：実装の順番がユーザー登録画面がメニュー画面より後だったため、リダイレクトせずに直接呼び出してしまったのかと思います。リダイレクトの処理を追加で記述し、パスワード変更画面、月別一覧画面と同じような形で実装をしました。

--------------------------------------------------------------------------------

□ KK06001Controller
・enterKK06001
	モデルにセットしている画面名がメニューとなっている

対応：モデルにセットしている画面名を"パスワード変更"に変更

・updateChangePassword
	20文字以上かどうかのチェックは不要？（htmlでのmaxlength指定は無い）

対応：htmlのパスワードの欄にmaxlength="20"を追加

・getoutFormKK06001
	getoutFromの間違い？

対応：スペルミスです。修正しました。

--------------------------------------------------------------------------------

□ KK06002Controller
・ロケーション・ユニット情報の登録は実装中 or 未コミットで良い？

対応：実装完了し、コミットしました

・registUser
	「if(KK06002Form.getUserId().equals(","))」の部分で社員番号に「,」が入ってくるのは仕様？

対応：検索ボックスの社員IDとユーザー登録の社員IDの値を格納する場所が同じだったため、カンマ区切りで格納されてしまっていたことが原因で実装をしておりました。現在はこの処理を消して別の処理で実装済みです。

--------------------------------------------------------------------------------



12/18 追記
--------------------------------------------------------------------------------

□ KK04001Controller
・enterKK04001
	該当月の初日から月末日をリストで取得するdo-while内で年と月をフォームにセットしているがループ外の1回ではだめなのか？

対応：年と月についてはdo-while文の外で問題ありませんした。ループ外に処理を変更しました。

・insertWorkDailyReport
	enterKK04001と同じ内容のものあり
 	「if (result.hasErrors())」の中で、モデルに管理者フラグを追加している記述が2つある。1つで良いのでは？

　対応：

・determin
	determineの間違い？

対応：スペルミスです。修正しました。


・edit
	determinでは月の値を整形していたが同様の処理は不要？
	リターン前にフォームに値設定しているが、KK03001Formに設定する必要がある？

対応：月の値の整形処理をeditにも追加しました。
　　　KK03001Formへの値の設定処理は削除しました。

--------------------------------------------------------------------------------

□ CommonController
・reroadKK04001
	メソッド名はreloadの間違い？（KK04001Controller.insertWorkDailyReportのリターンのパスも）

対応：スペルミスです。修正しました。

--------------------------------------------------------------------------------

